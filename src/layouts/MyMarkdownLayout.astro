---
import Footer from "../components/Footer.astro";
const { frontmatter } = Astro.props;
---

<h1>{frontmatter.title}</h1>
<p>著者: {frontmatter.author}（{frontmatter.pubDate.slice(0, 10)}に投稿）</p>
<slot />
<Footer />
